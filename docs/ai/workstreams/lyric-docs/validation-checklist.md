# Validation Checklist

## Structure

- The app is capability-driven rather than purely reference-driven.
- Each capability page shows authored input, generated output, and runtime effect.

## Utility

- The app is useful for both humans and agents.
- The app exposes generator problems instead of hiding them.
- The app can become a repeatable acceptance surface for v1 behavior.
