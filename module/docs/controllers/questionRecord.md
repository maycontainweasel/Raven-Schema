Title: QuestionRecord Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **QuestionRecord controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **QuestionRecord controller** is a front-end helper that wraps the TRPC router for the `questionRecord` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/questionRecord.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/questionRecord.ts`

Custom overrides (project side):
- `schema/controllers/questionRecord.ts` (or `app/schema/controllers/questionRecord.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedQuestionRecordController()`
- `extendQuestionRecordController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { QuestionRecordController } from '@schema/controllers/questionRecord'

export const extendQuestionRecordController = (base: QuestionRecordController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'qRecord', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
None

Optional fields:
- q
- attempts
- noCorrect
- noIncorrect
- percCorrect

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `questionRecord.create`

### update(id, payload)
- TRPC endpoint: `questionRecord.update`

### delete(id)
- TRPC endpoint: `questionRecord.delete`

### get(id, resourceKey)
- TRPC endpoint: `questionRecord.resource`
- Known resource keys: None

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `questionRecord.<taxonomy>.createTaxonomy`
- `questionRecord.<taxonomy>.addTerm`
- `questionRecord.<taxonomy>.removeTerm`
- `questionRecord.<taxonomy>.attach`
- `questionRecord.<taxonomy>.detach`
- `questionRecord.<taxonomy>.getTerms`
- `questionRecord.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `questionRecord.subtables.<child>.create`
- `questionRecord.subtables.<child>.update`
- `questionRecord.subtables.<child>.delete`
- `questionRecord.subtables.<child>.get`
- `questionRecord.subtables.<child>.list` (submany only)

This model is a subtable: yes (parent model: q)

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `questionRecord.relations.<relation>.attach`
- `questionRecord.relations.<relation>.detach`
- `questionRecord.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `questionRecord.typesense.resource`
- `questionRecord.typesense.list`
- `questionRecord.typesense.refresh`
- `questionRecord.typesense.count`
- `questionRecord.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/questionRecord.ts`

Wrapper router:
- `server/trpc/routers/questionRecord.ts`

---

End of document.
