Title: Announcement Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **Announcement controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **Announcement controller** is a front-end helper that wraps the TRPC router for the `announcement` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/announcement.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/announcement.ts`

Custom overrides (project side):
- `schema/controllers/announcement.ts` (or `app/schema/controllers/announcement.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedAnnouncementController()`
- `extendAnnouncementController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { AnnouncementController } from '@schema/controllers/announcement'

export const extendAnnouncementController = (base: AnnouncementController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'announcement', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- title (required)
- message (required)
- style (required)
- severity (required)
- dismissible (required)
- active (required)

Optional fields:
- startsAt
- endsAt
- instances

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `announcement.create`

### update(id, payload)
- TRPC endpoint: `announcement.update`

### delete(id)
- TRPC endpoint: `announcement.delete`

### get(id, resourceKey)
- TRPC endpoint: `announcement.resource`
- Known resource keys: Admin, Public

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `announcement.<taxonomy>.createTaxonomy`
- `announcement.<taxonomy>.addTerm`
- `announcement.<taxonomy>.removeTerm`
- `announcement.<taxonomy>.attach`
- `announcement.<taxonomy>.detach`
- `announcement.<taxonomy>.getTerms`
- `announcement.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `announcement.subtables.<child>.create`
- `announcement.subtables.<child>.update`
- `announcement.subtables.<child>.delete`
- `announcement.subtables.<child>.get`
- `announcement.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `announcement.relations.<relation>.attach`
- `announcement.relations.<relation>.detach`
- `announcement.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: yes

Endpoints (if enabled):
- `announcement.typesense.resource`
- `announcement.typesense.list`
- `announcement.typesense.refresh`
- `announcement.typesense.count`
- `announcement.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/announcement.ts`

Wrapper router:
- `server/trpc/routers/announcement.ts`

---

End of document.
