Title: Attempt Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **Attempt controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **Attempt controller** is a front-end helper that wraps the TRPC router for the `attempt` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/attempt.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/attempt.ts`

Custom overrides (project side):
- `schema/controllers/attempt.ts` (or `app/schema/controllers/attempt.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedAttemptController()`
- `extendAttemptController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { AttemptController } from '@schema/controllers/attempt'

export const extendAttemptController = (base: AttemptController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'attempt', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- s (required)
- q (required)
- u (required)
- selectedOptionID (required)

Optional fields:
- correct
- order
- totalTime
- exams
- examsTerms
- categories

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `attempt.create`

### update(id, payload)
- TRPC endpoint: `attempt.update`

### delete(id)
- TRPC endpoint: `attempt.delete`

### get(id, resourceKey)
- TRPC endpoint: `attempt.resource`
- Known resource keys: None

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `attempt.<taxonomy>.createTaxonomy`
- `attempt.<taxonomy>.addTerm`
- `attempt.<taxonomy>.removeTerm`
- `attempt.<taxonomy>.attach`
- `attempt.<taxonomy>.detach`
- `attempt.<taxonomy>.getTerms`
- `attempt.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `attempt.subtables.<child>.create`
- `attempt.subtables.<child>.update`
- `attempt.subtables.<child>.delete`
- `attempt.subtables.<child>.get`
- `attempt.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
- session

Endpoints:
- `attempt.relations.<relation>.attach`
- `attempt.relations.<relation>.detach`
- `attempt.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `attempt.typesense.resource`
- `attempt.typesense.list`
- `attempt.typesense.refresh`
- `attempt.typesense.count`
- `attempt.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/attempt.ts`

Wrapper router:
- `server/trpc/routers/attempt.ts`

---

End of document.
