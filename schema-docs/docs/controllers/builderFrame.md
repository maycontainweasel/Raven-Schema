Title: BuilderFrame Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **BuilderFrame controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **BuilderFrame controller** is a front-end helper that wraps the TRPC router for the `builderFrame` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/builderFrame.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/builderFrame.ts`

Custom overrides (project side):
- `schema/controllers/builderFrame.ts` (or `app/schema/controllers/builderFrame.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedBuilderFrameController()`
- `extendBuilderFrameController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { BuilderFrameController } from '@schema/controllers/builderFrame'

export const extendBuilderFrameController = (base: BuilderFrameController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'builderFrame', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- page (required)
- order (required)

Optional fields:
- parent
- kind
- className
- wrapperClass
- elementId

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `builderFrame.create`

### update(id, payload)
- TRPC endpoint: `builderFrame.update`

### delete(id)
- TRPC endpoint: `builderFrame.delete`

### get(id, resourceKey)
- TRPC endpoint: `builderFrame.resource`
- Known resource keys: Admin

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `builderFrame.<taxonomy>.createTaxonomy`
- `builderFrame.<taxonomy>.addTerm`
- `builderFrame.<taxonomy>.removeTerm`
- `builderFrame.<taxonomy>.attach`
- `builderFrame.<taxonomy>.detach`
- `builderFrame.<taxonomy>.getTerms`
- `builderFrame.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `builderFrame.subtables.<child>.create`
- `builderFrame.subtables.<child>.update`
- `builderFrame.subtables.<child>.delete`
- `builderFrame.subtables.<child>.get`
- `builderFrame.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
- builderFrame
- builderFrame
- builderWidget
- builderPage

Endpoints:
- `builderFrame.relations.<relation>.attach`
- `builderFrame.relations.<relation>.detach`
- `builderFrame.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `builderFrame.typesense.resource`
- `builderFrame.typesense.list`
- `builderFrame.typesense.refresh`
- `builderFrame.typesense.count`
- `builderFrame.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/builderFrame.ts`

Wrapper router:
- `server/trpc/routers/builderFrame.ts`

---

End of document.
