Title: BuilderPage Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **BuilderPage controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **BuilderPage controller** is a front-end helper that wraps the TRPC router for the `builderPage` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/builderPage.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/builderPage.ts`

Custom overrides (project side):
- `schema/controllers/builderPage.ts` (or `app/schema/controllers/builderPage.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedBuilderPageController()`
- `extendBuilderPageController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { BuilderPageController } from '@schema/controllers/builderPage'

export const extendBuilderPageController = (base: BuilderPageController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'builderPage', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
None

Optional fields:
- title

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `builderPage.create`

### update(id, payload)
- TRPC endpoint: `builderPage.update`

### delete(id)
- TRPC endpoint: `builderPage.delete`

### get(id, resourceKey)
- TRPC endpoint: `builderPage.resource`
- Known resource keys: Admin

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `builderPage.<taxonomy>.createTaxonomy`
- `builderPage.<taxonomy>.addTerm`
- `builderPage.<taxonomy>.removeTerm`
- `builderPage.<taxonomy>.attach`
- `builderPage.<taxonomy>.detach`
- `builderPage.<taxonomy>.getTerms`
- `builderPage.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `builderPage.subtables.<child>.create`
- `builderPage.subtables.<child>.update`
- `builderPage.subtables.<child>.delete`
- `builderPage.subtables.<child>.get`
- `builderPage.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
- builderFrame
- exam

Endpoints:
- `builderPage.relations.<relation>.attach`
- `builderPage.relations.<relation>.detach`
- `builderPage.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `builderPage.typesense.resource`
- `builderPage.typesense.list`
- `builderPage.typesense.refresh`
- `builderPage.typesense.count`
- `builderPage.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/builderPage.ts`

Wrapper router:
- `server/trpc/routers/builderPage.ts`

---

End of document.
