Title: BuilderWidget Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **BuilderWidget controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **BuilderWidget controller** is a front-end helper that wraps the TRPC router for the `builderWidget` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/builderWidget.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/builderWidget.ts`

Custom overrides (project side):
- `schema/controllers/builderWidget.ts` (or `app/schema/controllers/builderWidget.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedBuilderWidgetController()`
- `extendBuilderWidgetController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { BuilderWidgetController } from '@schema/controllers/builderWidget'

export const extendBuilderWidgetController = (base: BuilderWidgetController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'builderWidget', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- frame (required)
- order (required)
- type (required)

Optional fields:
- data
- className
- wrapperClass
- elementId

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `builderWidget.create`

### update(id, payload)
- TRPC endpoint: `builderWidget.update`

### delete(id)
- TRPC endpoint: `builderWidget.delete`

### get(id, resourceKey)
- TRPC endpoint: `builderWidget.resource`
- Known resource keys: Admin

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `builderWidget.<taxonomy>.createTaxonomy`
- `builderWidget.<taxonomy>.addTerm`
- `builderWidget.<taxonomy>.removeTerm`
- `builderWidget.<taxonomy>.attach`
- `builderWidget.<taxonomy>.detach`
- `builderWidget.<taxonomy>.getTerms`
- `builderWidget.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `builderWidget.subtables.<child>.create`
- `builderWidget.subtables.<child>.update`
- `builderWidget.subtables.<child>.delete`
- `builderWidget.subtables.<child>.get`
- `builderWidget.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
- builderFrame

Endpoints:
- `builderWidget.relations.<relation>.attach`
- `builderWidget.relations.<relation>.detach`
- `builderWidget.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `builderWidget.typesense.resource`
- `builderWidget.typesense.list`
- `builderWidget.typesense.refresh`
- `builderWidget.typesense.count`
- `builderWidget.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/builderWidget.ts`

Wrapper router:
- `server/trpc/routers/builderWidget.ts`

---

End of document.
