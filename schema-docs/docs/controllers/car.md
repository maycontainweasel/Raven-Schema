Title: Car Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **Car controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **Car controller** is a front-end helper that wraps the TRPC router for the `car` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/car.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/car.ts`

Custom overrides (project side):
- `schema/controllers/car.ts` (or `app/schema/controllers/car.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedCarController()`
- `extendCarController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { CarController } from '@schema/controllers/car'

export const extendCarController = (base: CarController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'car', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- carid (required)
- name (required)
- color (required)
- taste (required)
- price (required)
- quantity (required)

Optional fields:
None

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `car.create`

### update(id, payload)
- TRPC endpoint: `car.update`

### delete(id)
- TRPC endpoint: `car.delete`

### get(id, resourceKey)
- TRPC endpoint: `car.resource`
- Known resource keys: None

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `car.<taxonomy>.createTaxonomy`
- `car.<taxonomy>.addTerm`
- `car.<taxonomy>.removeTerm`
- `car.<taxonomy>.attach`
- `car.<taxonomy>.detach`
- `car.<taxonomy>.getTerms`
- `car.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `car.subtables.<child>.create`
- `car.subtables.<child>.update`
- `car.subtables.<child>.delete`
- `car.subtables.<child>.get`
- `car.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `car.relations.<relation>.attach`
- `car.relations.<relation>.detach`
- `car.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `car.typesense.resource`
- `car.typesense.list`
- `car.typesense.refresh`
- `car.typesense.count`
- `car.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/car.ts`

Wrapper router:
- `server/trpc/routers/car.ts`

---

End of document.
