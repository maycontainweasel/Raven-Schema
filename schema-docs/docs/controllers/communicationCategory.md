Title: CommunicationCategory Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **CommunicationCategory controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **CommunicationCategory controller** is a front-end helper that wraps the TRPC router for the `communicationCategory` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/communicationCategory.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/communicationCategory.ts`

Custom overrides (project side):
- `schema/controllers/communicationCategory.ts` (or `app/schema/controllers/communicationCategory.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedCommunicationCategoryController()`
- `extendCommunicationCategoryController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { CommunicationCategoryController } from '@schema/controllers/communicationCategory'

export const extendCommunicationCategoryController = (base: CommunicationCategoryController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'commCategory', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- key (required)
- label (required)
- sourceChannel (required)
- category (required)
- active (required)

Optional fields:
- description
- defaultTags
- order

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `communicationCategory.create`

### update(id, payload)
- TRPC endpoint: `communicationCategory.update`

### delete(id)
- TRPC endpoint: `communicationCategory.delete`

### get(id, resourceKey)
- TRPC endpoint: `communicationCategory.resource`
- Known resource keys: Admin

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `communicationCategory.<taxonomy>.createTaxonomy`
- `communicationCategory.<taxonomy>.addTerm`
- `communicationCategory.<taxonomy>.removeTerm`
- `communicationCategory.<taxonomy>.attach`
- `communicationCategory.<taxonomy>.detach`
- `communicationCategory.<taxonomy>.getTerms`
- `communicationCategory.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `communicationCategory.subtables.<child>.create`
- `communicationCategory.subtables.<child>.update`
- `communicationCategory.subtables.<child>.delete`
- `communicationCategory.subtables.<child>.get`
- `communicationCategory.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `communicationCategory.relations.<relation>.attach`
- `communicationCategory.relations.<relation>.detach`
- `communicationCategory.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `communicationCategory.typesense.resource`
- `communicationCategory.typesense.list`
- `communicationCategory.typesense.refresh`
- `communicationCategory.typesense.count`
- `communicationCategory.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/communicationCategory.ts`

Wrapper router:
- `server/trpc/routers/communicationCategory.ts`

---

End of document.
