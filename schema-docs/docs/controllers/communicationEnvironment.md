Title: CommunicationEnvironment Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **CommunicationEnvironment controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **CommunicationEnvironment controller** is a front-end helper that wraps the TRPC router for the `communicationEnvironment` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/communicationEnvironment.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/communicationEnvironment.ts`

Custom overrides (project side):
- `schema/controllers/communicationEnvironment.ts` (or `app/schema/controllers/communicationEnvironment.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedCommunicationEnvironmentController()`
- `extendCommunicationEnvironmentController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { CommunicationEnvironmentController } from '@schema/controllers/communicationEnvironment'

export const extendCommunicationEnvironmentController = (base: CommunicationEnvironmentController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'commEnv', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- key (required)
- label (required)
- active (required)

Optional fields:
- order

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `communicationEnvironment.create`

### update(id, payload)
- TRPC endpoint: `communicationEnvironment.update`

### delete(id)
- TRPC endpoint: `communicationEnvironment.delete`

### get(id, resourceKey)
- TRPC endpoint: `communicationEnvironment.resource`
- Known resource keys: Admin

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `communicationEnvironment.<taxonomy>.createTaxonomy`
- `communicationEnvironment.<taxonomy>.addTerm`
- `communicationEnvironment.<taxonomy>.removeTerm`
- `communicationEnvironment.<taxonomy>.attach`
- `communicationEnvironment.<taxonomy>.detach`
- `communicationEnvironment.<taxonomy>.getTerms`
- `communicationEnvironment.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `communicationEnvironment.subtables.<child>.create`
- `communicationEnvironment.subtables.<child>.update`
- `communicationEnvironment.subtables.<child>.delete`
- `communicationEnvironment.subtables.<child>.get`
- `communicationEnvironment.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `communicationEnvironment.relations.<relation>.attach`
- `communicationEnvironment.relations.<relation>.detach`
- `communicationEnvironment.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `communicationEnvironment.typesense.resource`
- `communicationEnvironment.typesense.list`
- `communicationEnvironment.typesense.refresh`
- `communicationEnvironment.typesense.count`
- `communicationEnvironment.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/communicationEnvironment.ts`

Wrapper router:
- `server/trpc/routers/communicationEnvironment.ts`

---

End of document.
