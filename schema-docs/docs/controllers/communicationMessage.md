Title: CommunicationMessage Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **CommunicationMessage controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **CommunicationMessage controller** is a front-end helper that wraps the TRPC router for the `communicationMessage` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/communicationMessage.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/communicationMessage.ts`

Custom overrides (project side):
- `schema/controllers/communicationMessage.ts` (or `app/schema/controllers/communicationMessage.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedCommunicationMessageController()`
- `extendCommunicationMessageController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { CommunicationMessageController } from '@schema/controllers/communicationMessage'

export const extendCommunicationMessageController = (base: CommunicationMessageController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'commMessage', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- thread (required)
- body (required)

Optional fields:
- direction
- authorProfile
- richBody
- metadata
- tags
- createdAt
- readAt

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `communicationMessage.create`

### update(id, payload)
- TRPC endpoint: `communicationMessage.update`

### delete(id)
- TRPC endpoint: `communicationMessage.delete`

### get(id, resourceKey)
- TRPC endpoint: `communicationMessage.resource`
- Known resource keys: Admin

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `communicationMessage.<taxonomy>.createTaxonomy`
- `communicationMessage.<taxonomy>.addTerm`
- `communicationMessage.<taxonomy>.removeTerm`
- `communicationMessage.<taxonomy>.attach`
- `communicationMessage.<taxonomy>.detach`
- `communicationMessage.<taxonomy>.getTerms`
- `communicationMessage.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `communicationMessage.subtables.<child>.create`
- `communicationMessage.subtables.<child>.update`
- `communicationMessage.subtables.<child>.delete`
- `communicationMessage.subtables.<child>.get`
- `communicationMessage.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `communicationMessage.relations.<relation>.attach`
- `communicationMessage.relations.<relation>.detach`
- `communicationMessage.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `communicationMessage.typesense.resource`
- `communicationMessage.typesense.list`
- `communicationMessage.typesense.refresh`
- `communicationMessage.typesense.count`
- `communicationMessage.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/communicationMessage.ts`

Wrapper router:
- `server/trpc/routers/communicationMessage.ts`

---

End of document.
