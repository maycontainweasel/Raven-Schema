Title: CommunicationThread Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **CommunicationThread controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **CommunicationThread controller** is a front-end helper that wraps the TRPC router for the `communicationThread` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/communicationThread.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/communicationThread.ts`

Custom overrides (project side):
- `schema/controllers/communicationThread.ts` (or `app/schema/controllers/communicationThread.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedCommunicationThreadController()`
- `extendCommunicationThreadController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { CommunicationThreadController } from '@schema/controllers/communicationThread'

export const extendCommunicationThreadController = (base: CommunicationThreadController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'commThread', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- subject (required)
- category (required)
- sourceChannel (required)
- sourceInstance (required)
- contactProfile (required)

Optional fields:
- subcategory
- tags
- sourceContext
- status
- priority
- assignedAdmin
- unreadAdminCount
- firstMessage
- lastMessage
- messageIds
- createdAt
- updatedAt
- lastMessageAt

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `communicationThread.create`

### update(id, payload)
- TRPC endpoint: `communicationThread.update`

### delete(id)
- TRPC endpoint: `communicationThread.delete`

### get(id, resourceKey)
- TRPC endpoint: `communicationThread.resource`
- Known resource keys: AdminList, AdminSingle

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `communicationThread.<taxonomy>.createTaxonomy`
- `communicationThread.<taxonomy>.addTerm`
- `communicationThread.<taxonomy>.removeTerm`
- `communicationThread.<taxonomy>.attach`
- `communicationThread.<taxonomy>.detach`
- `communicationThread.<taxonomy>.getTerms`
- `communicationThread.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `communicationThread.subtables.<child>.create`
- `communicationThread.subtables.<child>.update`
- `communicationThread.subtables.<child>.delete`
- `communicationThread.subtables.<child>.get`
- `communicationThread.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `communicationThread.relations.<relation>.attach`
- `communicationThread.relations.<relation>.detach`
- `communicationThread.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: yes

Endpoints (if enabled):
- `communicationThread.typesense.resource`
- `communicationThread.typesense.list`
- `communicationThread.typesense.refresh`
- `communicationThread.typesense.count`
- `communicationThread.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/communicationThread.ts`

Wrapper router:
- `server/trpc/routers/communicationThread.ts`

---

End of document.
