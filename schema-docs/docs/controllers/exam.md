Title: Exam Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **Exam controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **Exam controller** is a front-end helper that wraps the TRPC router for the `exam` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/exam.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/exam.ts`

Custom overrides (project side):
- `schema/controllers/exam.ts` (or `app/schema/controllers/exam.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedExamController()`
- `extendExamController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { ExamController } from '@schema/controllers/exam'

export const extendExamController = (base: ExamController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'exam', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- key (required)
- title (required)
- qidIndex (required)

Optional fields:
- titleShort
- description
- college
- sheetId
- order
- instances
- colours

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `exam.create`

### update(id, payload)
- TRPC endpoint: `exam.update`

### delete(id)
- TRPC endpoint: `exam.delete`

### get(id, resourceKey)
- TRPC endpoint: `exam.resource`
- Known resource keys: Admin, Public

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `exam.<taxonomy>.createTaxonomy`
- `exam.<taxonomy>.addTerm`
- `exam.<taxonomy>.removeTerm`
- `exam.<taxonomy>.attach`
- `exam.<taxonomy>.detach`
- `exam.<taxonomy>.getTerms`
- `exam.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `exam.subtables.<child>.create`
- `exam.subtables.<child>.update`
- `exam.subtables.<child>.delete`
- `exam.subtables.<child>.get`
- `exam.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
- question
- builderPage
- product
- question
- questionAttempt
- session

Endpoints:
- `exam.relations.<relation>.attach`
- `exam.relations.<relation>.detach`
- `exam.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: yes

Endpoints (if enabled):
- `exam.typesense.resource`
- `exam.typesense.list`
- `exam.typesense.refresh`
- `exam.typesense.count`
- `exam.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/exam.ts`

Wrapper router:
- `server/trpc/routers/exam.ts`

---

End of document.
