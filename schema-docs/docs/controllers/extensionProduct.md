Title: ExtensionProduct Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **ExtensionProduct controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **ExtensionProduct controller** is a front-end helper that wraps the TRPC router for the `extensionProduct` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/extensionProduct.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/extensionProduct.ts`

Custom overrides (project side):
- `schema/controllers/extensionProduct.ts` (or `app/schema/controllers/extensionProduct.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedExtensionProductController()`
- `extendExtensionProductController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { ExtensionProductController } from '@schema/controllers/extensionProduct'

export const extendExtensionProductController = (base: ExtensionProductController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'extensionProduct', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- productKey (required)
- examKey (required)
- product (required)
- exam (required)
- productVariant (required)
- period (required)
- currency (required)

Optional fields:
- stripePID

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `extensionProduct.create`

### update(id, payload)
- TRPC endpoint: `extensionProduct.update`

### delete(id)
- TRPC endpoint: `extensionProduct.delete`

### get(id, resourceKey)
- TRPC endpoint: `extensionProduct.resource`
- Known resource keys: None

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `extensionProduct.<taxonomy>.createTaxonomy`
- `extensionProduct.<taxonomy>.addTerm`
- `extensionProduct.<taxonomy>.removeTerm`
- `extensionProduct.<taxonomy>.attach`
- `extensionProduct.<taxonomy>.detach`
- `extensionProduct.<taxonomy>.getTerms`
- `extensionProduct.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `extensionProduct.subtables.<child>.create`
- `extensionProduct.subtables.<child>.update`
- `extensionProduct.subtables.<child>.delete`
- `extensionProduct.subtables.<child>.get`
- `extensionProduct.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
- extensionVariant
- order
- productVariant

Endpoints:
- `extensionProduct.relations.<relation>.attach`
- `extensionProduct.relations.<relation>.detach`
- `extensionProduct.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `extensionProduct.typesense.resource`
- `extensionProduct.typesense.list`
- `extensionProduct.typesense.refresh`
- `extensionProduct.typesense.count`
- `extensionProduct.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/extensionProduct.ts`

Wrapper router:
- `server/trpc/routers/extensionProduct.ts`

---

End of document.
