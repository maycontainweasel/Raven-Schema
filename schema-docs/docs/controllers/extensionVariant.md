Title: ExtensionVariant Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **ExtensionVariant controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **ExtensionVariant controller** is a front-end helper that wraps the TRPC router for the `extensionVariant` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/extensionVariant.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/extensionVariant.ts`

Custom overrides (project side):
- `schema/controllers/extensionVariant.ts` (or `app/schema/controllers/extensionVariant.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedExtensionVariantController()`
- `extendExtensionVariantController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { ExtensionVariantController } from '@schema/controllers/extensionVariant'

export const extendExtensionVariantController = (base: ExtensionVariantController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'extensionVariant', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- productKey (required)
- examKey (required)
- product (required)
- exam (required)
- productVariant (required)
- extensionProduct (required)
- period (required)
- currency (required)
- numberOfDays (required)
- value (required)

Optional fields:
- stripePriceID

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `extensionVariant.create`

### update(id, payload)
- TRPC endpoint: `extensionVariant.update`

### delete(id)
- TRPC endpoint: `extensionVariant.delete`

### get(id, resourceKey)
- TRPC endpoint: `extensionVariant.resource`
- Known resource keys: None

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `extensionVariant.<taxonomy>.createTaxonomy`
- `extensionVariant.<taxonomy>.addTerm`
- `extensionVariant.<taxonomy>.removeTerm`
- `extensionVariant.<taxonomy>.attach`
- `extensionVariant.<taxonomy>.detach`
- `extensionVariant.<taxonomy>.getTerms`
- `extensionVariant.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `extensionVariant.subtables.<child>.create`
- `extensionVariant.subtables.<child>.update`
- `extensionVariant.subtables.<child>.delete`
- `extensionVariant.subtables.<child>.get`
- `extensionVariant.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
- extensionProduct
- order

Endpoints:
- `extensionVariant.relations.<relation>.attach`
- `extensionVariant.relations.<relation>.detach`
- `extensionVariant.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `extensionVariant.typesense.resource`
- `extensionVariant.typesense.list`
- `extensionVariant.typesense.refresh`
- `extensionVariant.typesense.count`
- `extensionVariant.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/extensionVariant.ts`

Wrapper router:
- `server/trpc/routers/extensionVariant.ts`

---

End of document.
