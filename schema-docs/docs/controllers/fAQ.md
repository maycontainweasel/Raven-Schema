Title: FAQ Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **FAQ controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **FAQ controller** is a front-end helper that wraps the TRPC router for the `fAQ` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/fAQ.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/fAQ.ts`

Custom overrides (project side):
- `schema/controllers/fAQ.ts` (or `app/schema/controllers/fAQ.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedFAQController()`
- `extendFAQController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { FAQController } from '@schema/controllers/fAQ'

export const extendFAQController = (base: FAQController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'faq', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- question (required)
- answer (required)
- category (required)

Optional fields:
- faqCategorys
- instances

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `fAQ.create`

### update(id, payload)
- TRPC endpoint: `fAQ.update`

### delete(id)
- TRPC endpoint: `fAQ.delete`

### get(id, resourceKey)
- TRPC endpoint: `fAQ.resource`
- Known resource keys: Admin, Public

---

## 7) Taxonomy Helper

Available taxonomies:
- faqCategory

Endpoints:
- `fAQ.<taxonomy>.createTaxonomy`
- `fAQ.<taxonomy>.addTerm`
- `fAQ.<taxonomy>.removeTerm`
- `fAQ.<taxonomy>.attach`
- `fAQ.<taxonomy>.detach`
- `fAQ.<taxonomy>.getTerms`
- `fAQ.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `fAQ.subtables.<child>.create`
- `fAQ.subtables.<child>.update`
- `fAQ.subtables.<child>.delete`
- `fAQ.subtables.<child>.get`
- `fAQ.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `fAQ.relations.<relation>.attach`
- `fAQ.relations.<relation>.detach`
- `fAQ.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: yes

Endpoints (if enabled):
- `fAQ.typesense.resource`
- `fAQ.typesense.list`
- `fAQ.typesense.refresh`
- `fAQ.typesense.count`
- `fAQ.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/fAQ.ts`

Wrapper router:
- `server/trpc/routers/fAQ.ts`

---

End of document.
