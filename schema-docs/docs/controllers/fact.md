Title: Fact Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **Fact controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **Fact controller** is a front-end helper that wraps the TRPC router for the `fact` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/fact.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/fact.ts`

Custom overrides (project side):
- `schema/controllers/fact.ts` (or `app/schema/controllers/fact.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedFactController()`
- `extendFactController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { FactController } from '@schema/controllers/fact'

export const extendFactController = (base: FactController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'fact', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- title (required)
- body (required)
- active (required)

Optional fields:
- source
- order
- instances

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `fact.create`

### update(id, payload)
- TRPC endpoint: `fact.update`

### delete(id)
- TRPC endpoint: `fact.delete`

### get(id, resourceKey)
- TRPC endpoint: `fact.resource`
- Known resource keys: Admin, Public

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `fact.<taxonomy>.createTaxonomy`
- `fact.<taxonomy>.addTerm`
- `fact.<taxonomy>.removeTerm`
- `fact.<taxonomy>.attach`
- `fact.<taxonomy>.detach`
- `fact.<taxonomy>.getTerms`
- `fact.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `fact.subtables.<child>.create`
- `fact.subtables.<child>.update`
- `fact.subtables.<child>.delete`
- `fact.subtables.<child>.get`
- `fact.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `fact.relations.<relation>.attach`
- `fact.relations.<relation>.detach`
- `fact.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: yes

Endpoints (if enabled):
- `fact.typesense.resource`
- `fact.typesense.list`
- `fact.typesense.refresh`
- `fact.typesense.count`
- `fact.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/fact.ts`

Wrapper router:
- `server/trpc/routers/fact.ts`

---

End of document.
