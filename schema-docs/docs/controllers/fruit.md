Title: Fruit Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **Fruit controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **Fruit controller** is a front-end helper that wraps the TRPC router for the `fruit` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/fruit.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/fruit.ts`

Custom overrides (project side):
- `schema/controllers/fruit.ts` (or `app/schema/controllers/fruit.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedFruitController()`
- `extendFruitController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { FruitController } from '@schema/controllers/fruit'

export const extendFruitController = (base: FruitController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'fruit', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- fruitid (required)
- name (required)
- color (required)
- taste (required)
- price (required)
- quantity (required)

Optional fields:
None

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `fruit.create`

### update(id, payload)
- TRPC endpoint: `fruit.update`

### delete(id)
- TRPC endpoint: `fruit.delete`

### get(id, resourceKey)
- TRPC endpoint: `fruit.resource`
- Known resource keys: None

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `fruit.<taxonomy>.createTaxonomy`
- `fruit.<taxonomy>.addTerm`
- `fruit.<taxonomy>.removeTerm`
- `fruit.<taxonomy>.attach`
- `fruit.<taxonomy>.detach`
- `fruit.<taxonomy>.getTerms`
- `fruit.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `fruit.subtables.<child>.create`
- `fruit.subtables.<child>.update`
- `fruit.subtables.<child>.delete`
- `fruit.subtables.<child>.get`
- `fruit.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `fruit.relations.<relation>.attach`
- `fruit.relations.<relation>.detach`
- `fruit.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `fruit.typesense.resource`
- `fruit.typesense.list`
- `fruit.typesense.refresh`
- `fruit.typesense.count`
- `fruit.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/fruit.ts`

Wrapper router:
- `server/trpc/routers/fruit.ts`

---

End of document.
