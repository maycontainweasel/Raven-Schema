Title: Instance Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **Instance controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **Instance controller** is a front-end helper that wraps the TRPC router for the `instance` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/instance.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/instance.ts`

Custom overrides (project side):
- `schema/controllers/instance.ts` (or `app/schema/controllers/instance.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedInstanceController()`
- `extendInstanceController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { InstanceController } from '@schema/controllers/instance'

export const extendInstanceController = (base: InstanceController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'instance', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- key (required)
- title (required)

Optional fields:
- instance
- status
- active

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `instance.create`

### update(id, payload)
- TRPC endpoint: `instance.update`

### delete(id)
- TRPC endpoint: `instance.delete`

### get(id, resourceKey)
- TRPC endpoint: `instance.resource`
- Known resource keys: Admin

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `instance.<taxonomy>.createTaxonomy`
- `instance.<taxonomy>.addTerm`
- `instance.<taxonomy>.removeTerm`
- `instance.<taxonomy>.attach`
- `instance.<taxonomy>.detach`
- `instance.<taxonomy>.getTerms`
- `instance.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
- settings

Endpoints:
- `instance.subtables.<child>.create`
- `instance.subtables.<child>.update`
- `instance.subtables.<child>.delete`
- `instance.subtables.<child>.get`
- `instance.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `instance.relations.<relation>.attach`
- `instance.relations.<relation>.detach`
- `instance.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `instance.typesense.resource`
- `instance.typesense.list`
- `instance.typesense.refresh`
- `instance.typesense.count`
- `instance.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/instance.ts`

Wrapper router:
- `server/trpc/routers/instance.ts`

---

End of document.
