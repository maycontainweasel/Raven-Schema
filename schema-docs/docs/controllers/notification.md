Title: Notification Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **Notification controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **Notification controller** is a front-end helper that wraps the TRPC router for the `notification` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/notification.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/notification.ts`

Custom overrides (project side):
- `schema/controllers/notification.ts` (or `app/schema/controllers/notification.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedNotificationController()`
- `extendNotificationController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { NotificationController } from '@schema/controllers/notification'

export const extendNotificationController = (base: NotificationController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'notification', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- title (required)
- body (required)
- active (required)

Optional fields:
- summary
- image
- ctaLabel
- ctaUrl
- publishAt
- order
- instances

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `notification.create`

### update(id, payload)
- TRPC endpoint: `notification.update`

### delete(id)
- TRPC endpoint: `notification.delete`

### get(id, resourceKey)
- TRPC endpoint: `notification.resource`
- Known resource keys: Admin, Public

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `notification.<taxonomy>.createTaxonomy`
- `notification.<taxonomy>.addTerm`
- `notification.<taxonomy>.removeTerm`
- `notification.<taxonomy>.attach`
- `notification.<taxonomy>.detach`
- `notification.<taxonomy>.getTerms`
- `notification.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `notification.subtables.<child>.create`
- `notification.subtables.<child>.update`
- `notification.subtables.<child>.delete`
- `notification.subtables.<child>.get`
- `notification.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `notification.relations.<relation>.attach`
- `notification.relations.<relation>.detach`
- `notification.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: yes

Endpoints (if enabled):
- `notification.typesense.resource`
- `notification.typesense.list`
- `notification.typesense.refresh`
- `notification.typesense.count`
- `notification.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/notification.ts`

Wrapper router:
- `server/trpc/routers/notification.ts`

---

End of document.
