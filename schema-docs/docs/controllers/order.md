Title: Order Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **Order controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **Order controller** is a front-end helper that wraps the TRPC router for the `order` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/order.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/order.ts`

Custom overrides (project side):
- `schema/controllers/order.ts` (or `app/schema/controllers/order.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedOrderController()`
- `extendOrderController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { OrderController } from '@schema/controllers/order'

export const extendOrderController = (base: OrderController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'order', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- u (required)

Optional fields:
- uSubscription
- exam
- productID
- variantID
- extensionID
- type
- status
- currency
- amountSubtotal
- amountTax
- amountDiscount
- amountTotal
- amountRefunded
- stripeID
- stripeOrderID
- paymentIntentID
- checkoutSessionID
- chargeID
- customerID
- invoiceID
- paymentMethodID
- stripePriceID
- stripeProductID
- subscriptionID
- receiptURL
- couponApplied
- couponCode
- items
- access
- userSnapshot
- paymentSnapshot
- events
- metadata
- stripePayload
- createdAt
- updatedAt
- instances

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `order.create`

### update(id, payload)
- TRPC endpoint: `order.update`

### delete(id)
- TRPC endpoint: `order.delete`

### get(id, resourceKey)
- TRPC endpoint: `order.resource`
- Known resource keys: Admin

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `order.<taxonomy>.createTaxonomy`
- `order.<taxonomy>.addTerm`
- `order.<taxonomy>.removeTerm`
- `order.<taxonomy>.attach`
- `order.<taxonomy>.detach`
- `order.<taxonomy>.getTerms`
- `order.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `order.subtables.<child>.create`
- `order.subtables.<child>.update`
- `order.subtables.<child>.delete`
- `order.subtables.<child>.get`
- `order.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
- product
- productVariant
- extensionProduct
- extensionVariant
- product
- product

Endpoints:
- `order.relations.<relation>.attach`
- `order.relations.<relation>.detach`
- `order.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: yes

Endpoints (if enabled):
- `order.typesense.resource`
- `order.typesense.list`
- `order.typesense.refresh`
- `order.typesense.count`
- `order.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/order.ts`

Wrapper router:
- `server/trpc/routers/order.ts`

---

End of document.
