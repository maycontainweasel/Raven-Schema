Title: OrderEvent Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **OrderEvent controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **OrderEvent controller** is a front-end helper that wraps the TRPC router for the `orderEvent` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/orderEvent.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/orderEvent.ts`

Custom overrides (project side):
- `schema/controllers/orderEvent.ts` (or `app/schema/controllers/orderEvent.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedOrderEventController()`
- `extendOrderEventController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { OrderEventController } from '@schema/controllers/orderEvent'

export const extendOrderEventController = (base: OrderEventController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'orderEvent', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- type (required)
- title (required)
- occurredAt (required)

Optional fields:
- description
- actor
- severity
- status
- code
- metadata
- order

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `orderEvent.create`

### update(id, payload)
- TRPC endpoint: `orderEvent.update`

### delete(id)
- TRPC endpoint: `orderEvent.delete`

### get(id, resourceKey)
- TRPC endpoint: `orderEvent.resource`
- Known resource keys: None

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `orderEvent.<taxonomy>.createTaxonomy`
- `orderEvent.<taxonomy>.addTerm`
- `orderEvent.<taxonomy>.removeTerm`
- `orderEvent.<taxonomy>.attach`
- `orderEvent.<taxonomy>.detach`
- `orderEvent.<taxonomy>.getTerms`
- `orderEvent.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `orderEvent.subtables.<child>.create`
- `orderEvent.subtables.<child>.update`
- `orderEvent.subtables.<child>.delete`
- `orderEvent.subtables.<child>.get`
- `orderEvent.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `orderEvent.relations.<relation>.attach`
- `orderEvent.relations.<relation>.detach`
- `orderEvent.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `orderEvent.typesense.resource`
- `orderEvent.typesense.list`
- `orderEvent.typesense.refresh`
- `orderEvent.typesense.count`
- `orderEvent.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/orderEvent.ts`

Wrapper router:
- `server/trpc/routers/orderEvent.ts`

---

End of document.
