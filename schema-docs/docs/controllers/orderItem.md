Title: OrderItem Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **OrderItem controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **OrderItem controller** is a front-end helper that wraps the TRPC router for the `orderItem` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/orderItem.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/orderItem.ts`

Custom overrides (project side):
- `schema/controllers/orderItem.ts` (or `app/schema/controllers/orderItem.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedOrderItemController()`
- `extendOrderItemController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { OrderItemController } from '@schema/controllers/orderItem'

export const extendOrderItemController = (base: OrderItemController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'orderItem', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- kind (required)

Optional fields:
- refTable
- refId
- label
- description
- quantity
- unitAmount
- totalAmount
- currency
- period
- numberOfDays
- stripePriceID
- instance
- metadata
- order

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `orderItem.create`

### update(id, payload)
- TRPC endpoint: `orderItem.update`

### delete(id)
- TRPC endpoint: `orderItem.delete`

### get(id, resourceKey)
- TRPC endpoint: `orderItem.resource`
- Known resource keys: None

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `orderItem.<taxonomy>.createTaxonomy`
- `orderItem.<taxonomy>.addTerm`
- `orderItem.<taxonomy>.removeTerm`
- `orderItem.<taxonomy>.attach`
- `orderItem.<taxonomy>.detach`
- `orderItem.<taxonomy>.getTerms`
- `orderItem.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `orderItem.subtables.<child>.create`
- `orderItem.subtables.<child>.update`
- `orderItem.subtables.<child>.delete`
- `orderItem.subtables.<child>.get`
- `orderItem.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `orderItem.relations.<relation>.attach`
- `orderItem.relations.<relation>.detach`
- `orderItem.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `orderItem.typesense.resource`
- `orderItem.typesense.list`
- `orderItem.typesense.refresh`
- `orderItem.typesense.count`
- `orderItem.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/orderItem.ts`

Wrapper router:
- `server/trpc/routers/orderItem.ts`

---

End of document.
