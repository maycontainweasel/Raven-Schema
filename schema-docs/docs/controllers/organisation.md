Title: Organisation Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **Organisation controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **Organisation controller** is a front-end helper that wraps the TRPC router for the `organisation` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/organisation.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/organisation.ts`

Custom overrides (project side):
- `schema/controllers/organisation.ts` (or `app/schema/controllers/organisation.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedOrganisationController()`
- `extendOrganisationController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { OrganisationController } from '@schema/controllers/organisation'

export const extendOrganisationController = (base: OrganisationController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'organisation', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- title (required)
- slug (required)
- active (required)

Optional fields:
- description
- website
- email
- phone
- industry
- size
- logo
- address
- instances
- createdAt
- updatedAt

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `organisation.create`

### update(id, payload)
- TRPC endpoint: `organisation.update`

### delete(id)
- TRPC endpoint: `organisation.delete`

### get(id, resourceKey)
- TRPC endpoint: `organisation.resource`
- Known resource keys: Admin

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `organisation.<taxonomy>.createTaxonomy`
- `organisation.<taxonomy>.addTerm`
- `organisation.<taxonomy>.removeTerm`
- `organisation.<taxonomy>.attach`
- `organisation.<taxonomy>.detach`
- `organisation.<taxonomy>.getTerms`
- `organisation.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `organisation.subtables.<child>.create`
- `organisation.subtables.<child>.update`
- `organisation.subtables.<child>.delete`
- `organisation.subtables.<child>.get`
- `organisation.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
- user

Endpoints:
- `organisation.relations.<relation>.attach`
- `organisation.relations.<relation>.detach`
- `organisation.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: yes

Endpoints (if enabled):
- `organisation.typesense.resource`
- `organisation.typesense.list`
- `organisation.typesense.refresh`
- `organisation.typesense.count`
- `organisation.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/organisation.ts`

Wrapper router:
- `server/trpc/routers/organisation.ts`

---

End of document.
