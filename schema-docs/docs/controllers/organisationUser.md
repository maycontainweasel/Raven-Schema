Title: OrganisationUser Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **OrganisationUser controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **OrganisationUser controller** is a front-end helper that wraps the TRPC router for the `organisationUser` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/organisationUser.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/organisationUser.ts`

Custom overrides (project side):
- `schema/controllers/organisationUser.ts` (or `app/schema/controllers/organisationUser.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedOrganisationUserController()`
- `extendOrganisationUserController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { OrganisationUserController } from '@schema/controllers/organisationUser'

export const extendOrganisationUserController = (base: OrganisationUserController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'OrganisationUsers', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- organisation (required)
- u (required)
- role (required)

Optional fields:
- addedAt

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `organisationUser.create`

### update(id, payload)
- TRPC endpoint: `organisationUser.update`

### delete(id)
- TRPC endpoint: `organisationUser.delete`

### get(id, resourceKey)
- TRPC endpoint: `organisationUser.resource`
- Known resource keys: None

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `organisationUser.<taxonomy>.createTaxonomy`
- `organisationUser.<taxonomy>.addTerm`
- `organisationUser.<taxonomy>.removeTerm`
- `organisationUser.<taxonomy>.attach`
- `organisationUser.<taxonomy>.detach`
- `organisationUser.<taxonomy>.getTerms`
- `organisationUser.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `organisationUser.subtables.<child>.create`
- `organisationUser.subtables.<child>.update`
- `organisationUser.subtables.<child>.delete`
- `organisationUser.subtables.<child>.get`
- `organisationUser.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `organisationUser.relations.<relation>.attach`
- `organisationUser.relations.<relation>.detach`
- `organisationUser.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `organisationUser.typesense.resource`
- `organisationUser.typesense.list`
- `organisationUser.typesense.refresh`
- `organisationUser.typesense.count`
- `organisationUser.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/organisationUser.ts`

Wrapper router:
- `server/trpc/routers/organisationUser.ts`

---

End of document.
