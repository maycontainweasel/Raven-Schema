Title: PMInstance Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **PMInstance controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **PMInstance controller** is a front-end helper that wraps the TRPC router for the `pMInstance` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/pMInstance.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/pMInstance.ts`

Custom overrides (project side):
- `schema/controllers/pMInstance.ts` (or `app/schema/controllers/pMInstance.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedPMInstanceController()`
- `extendPMInstanceController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { PMInstanceController } from '@schema/controllers/pMInstance'

export const extendPMInstanceController = (base: PMInstanceController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'pmInstance', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- key (required)
- instance (required)
- title (required)
- status (required)
- active (required)

Optional fields:
- flag
- description
- domains
- primaryDomain
- locale
- timezone
- currency
- currencies
- pricingOptions
- apiKeys
- surrealdb
- redis
- content
- createdAt
- updatedAt
- createdBy
- instances

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `pMInstance.create`

### update(id, payload)
- TRPC endpoint: `pMInstance.update`

### delete(id)
- TRPC endpoint: `pMInstance.delete`

### get(id, resourceKey)
- TRPC endpoint: `pMInstance.resource`
- Known resource keys: Admin

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `pMInstance.<taxonomy>.createTaxonomy`
- `pMInstance.<taxonomy>.addTerm`
- `pMInstance.<taxonomy>.removeTerm`
- `pMInstance.<taxonomy>.attach`
- `pMInstance.<taxonomy>.detach`
- `pMInstance.<taxonomy>.getTerms`
- `pMInstance.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `pMInstance.subtables.<child>.create`
- `pMInstance.subtables.<child>.update`
- `pMInstance.subtables.<child>.delete`
- `pMInstance.subtables.<child>.get`
- `pMInstance.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `pMInstance.relations.<relation>.attach`
- `pMInstance.relations.<relation>.detach`
- `pMInstance.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `pMInstance.typesense.resource`
- `pMInstance.typesense.list`
- `pMInstance.typesense.refresh`
- `pMInstance.typesense.count`
- `pMInstance.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/pMInstance.ts`

Wrapper router:
- `server/trpc/routers/pMInstance.ts`

---

End of document.
