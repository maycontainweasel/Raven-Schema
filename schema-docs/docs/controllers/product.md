Title: Product Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **Product controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **Product controller** is a front-end helper that wraps the TRPC router for the `product` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/product.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/product.ts`

Custom overrides (project side):
- `schema/controllers/product.ts` (or `app/schema/controllers/product.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedProductController()`
- `extendProductController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { ProductController } from '@schema/controllers/product'

export const extendProductController = (base: ProductController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'product', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- key (required)
- title (required)

Optional fields:
- slug
- description
- stripePID
- instances

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `product.create`

### update(id, payload)
- TRPC endpoint: `product.update`

### delete(id)
- TRPC endpoint: `product.delete`

### get(id, resourceKey)
- TRPC endpoint: `product.resource`
- Known resource keys: Admin

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `product.<taxonomy>.createTaxonomy`
- `product.<taxonomy>.addTerm`
- `product.<taxonomy>.removeTerm`
- `product.<taxonomy>.attach`
- `product.<taxonomy>.detach`
- `product.<taxonomy>.getTerms`
- `product.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `product.subtables.<child>.create`
- `product.subtables.<child>.update`
- `product.subtables.<child>.delete`
- `product.subtables.<child>.get`
- `product.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
- order
- order
- order
- exam
- productVariant

Endpoints:
- `product.relations.<relation>.attach`
- `product.relations.<relation>.detach`
- `product.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: yes

Endpoints (if enabled):
- `product.typesense.resource`
- `product.typesense.list`
- `product.typesense.refresh`
- `product.typesense.count`
- `product.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/product.ts`

Wrapper router:
- `server/trpc/routers/product.ts`

---

End of document.
