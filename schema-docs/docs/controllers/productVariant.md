Title: ProductVariant Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **ProductVariant controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **ProductVariant controller** is a front-end helper that wraps the TRPC router for the `productVariant` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/productVariant.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/productVariant.ts`

Custom overrides (project side):
- `schema/controllers/productVariant.ts` (or `app/schema/controllers/productVariant.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedProductVariantController()`
- `extendProductVariantController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { ProductVariantController } from '@schema/controllers/productVariant'

export const extendProductVariantController = (base: ProductVariantController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'productVariant', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- productKey (required)
- examKey (required)
- product (required)
- exam (required)
- period (required)
- currency (required)
- value (required)

Optional fields:
- label
- stripePriceID
- order

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `productVariant.create`

### update(id, payload)
- TRPC endpoint: `productVariant.update`

### delete(id)
- TRPC endpoint: `productVariant.delete`

### get(id, resourceKey)
- TRPC endpoint: `productVariant.resource`
- Known resource keys: None

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `productVariant.<taxonomy>.createTaxonomy`
- `productVariant.<taxonomy>.addTerm`
- `productVariant.<taxonomy>.removeTerm`
- `productVariant.<taxonomy>.attach`
- `productVariant.<taxonomy>.detach`
- `productVariant.<taxonomy>.getTerms`
- `productVariant.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `productVariant.subtables.<child>.create`
- `productVariant.subtables.<child>.update`
- `productVariant.subtables.<child>.delete`
- `productVariant.subtables.<child>.get`
- `productVariant.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
- order
- product
- extensionProduct

Endpoints:
- `productVariant.relations.<relation>.attach`
- `productVariant.relations.<relation>.detach`
- `productVariant.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `productVariant.typesense.resource`
- `productVariant.typesense.list`
- `productVariant.typesense.refresh`
- `productVariant.typesense.count`
- `productVariant.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/productVariant.ts`

Wrapper router:
- `server/trpc/routers/productVariant.ts`

---

End of document.
