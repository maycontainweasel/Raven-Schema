Title: Question Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **Question controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **Question controller** is a front-end helper that wraps the TRPC router for the `question` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/question.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/question.ts`

Custom overrides (project side):
- `schema/controllers/question.ts` (or `app/schema/controllers/question.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedQuestionController()`
- `extendQuestionController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { QuestionController } from '@schema/controllers/question'

export const extendQuestionController = (base: QuestionController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'q', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- qid (required)
- question (required)

Optional fields:
- qCode
- explanation
- explanationRef
- source
- instances
- testobject
- tags

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `question.create`

### update(id, payload)
- TRPC endpoint: `question.update`

### delete(id)
- TRPC endpoint: `question.delete`

### get(id, resourceKey)
- TRPC endpoint: `question.resource`
- Known resource keys: Admin

---

## 7) Taxonomy Helper

Available taxonomies:
- qcat
- qtag

Endpoints:
- `question.<taxonomy>.createTaxonomy`
- `question.<taxonomy>.addTerm`
- `question.<taxonomy>.removeTerm`
- `question.<taxonomy>.attach`
- `question.<taxonomy>.detach`
- `question.<taxonomy>.getTerms`
- `question.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
- questionRecord

Endpoints:
- `question.subtables.<child>.create`
- `question.subtables.<child>.update`
- `question.subtables.<child>.delete`
- `question.subtables.<child>.get`
- `question.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `question.relations.<relation>.attach`
- `question.relations.<relation>.detach`
- `question.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `question.typesense.resource`
- `question.typesense.list`
- `question.typesense.refresh`
- `question.typesense.count`
- `question.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/question.ts`

Wrapper router:
- `server/trpc/routers/question.ts`

---

End of document.
