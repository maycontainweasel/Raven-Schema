Title: QuestionAttempt Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **QuestionAttempt controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **QuestionAttempt controller** is a front-end helper that wraps the TRPC router for the `questionAttempt` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/questionAttempt.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/questionAttempt.ts`

Custom overrides (project side):
- `schema/controllers/questionAttempt.ts` (or `app/schema/controllers/questionAttempt.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedQuestionAttemptController()`
- `extendQuestionAttemptController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { QuestionAttemptController } from '@schema/controllers/questionAttempt'

export const extendQuestionAttemptController = (base: QuestionAttemptController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'qa', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- q (required)
- u (required)
- selectedOptionID (required)

Optional fields:
- s
- correct
- order
- totalTime
- exams
- examsTerms
- categories
- categoryIds

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `questionAttempt.create`

### update(id, payload)
- TRPC endpoint: `questionAttempt.update`

### delete(id)
- TRPC endpoint: `questionAttempt.delete`

### get(id, resourceKey)
- TRPC endpoint: `questionAttempt.resource`
- Known resource keys: None

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `questionAttempt.<taxonomy>.createTaxonomy`
- `questionAttempt.<taxonomy>.addTerm`
- `questionAttempt.<taxonomy>.removeTerm`
- `questionAttempt.<taxonomy>.attach`
- `questionAttempt.<taxonomy>.detach`
- `questionAttempt.<taxonomy>.getTerms`
- `questionAttempt.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `questionAttempt.subtables.<child>.create`
- `questionAttempt.subtables.<child>.update`
- `questionAttempt.subtables.<child>.delete`
- `questionAttempt.subtables.<child>.get`
- `questionAttempt.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
- user
- question
- session
- exam
- qt
- session

Endpoints:
- `questionAttempt.relations.<relation>.attach`
- `questionAttempt.relations.<relation>.detach`
- `questionAttempt.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `questionAttempt.typesense.resource`
- `questionAttempt.typesense.list`
- `questionAttempt.typesense.refresh`
- `questionAttempt.typesense.count`
- `questionAttempt.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/questionAttempt.ts`

Wrapper router:
- `server/trpc/routers/questionAttempt.ts`

---

End of document.
