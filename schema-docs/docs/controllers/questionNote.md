Title: QuestionNote Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **QuestionNote controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **QuestionNote controller** is a front-end helper that wraps the TRPC router for the `questionNote` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/questionNote.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/questionNote.ts`

Custom overrides (project side):
- `schema/controllers/questionNote.ts` (or `app/schema/controllers/questionNote.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedQuestionNoteController()`
- `extendQuestionNoteController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { QuestionNoteController } from '@schema/controllers/questionNote'

export const extendQuestionNoteController = (base: QuestionNoteController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'qnote', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- q (required)
- createdAt (required)
- createdBy (required)
- createdByName (required)

Optional fields:
- note
- order

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `questionNote.create`

### update(id, payload)
- TRPC endpoint: `questionNote.update`

### delete(id)
- TRPC endpoint: `questionNote.delete`

### get(id, resourceKey)
- TRPC endpoint: `questionNote.resource`
- Known resource keys: None

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `questionNote.<taxonomy>.createTaxonomy`
- `questionNote.<taxonomy>.addTerm`
- `questionNote.<taxonomy>.removeTerm`
- `questionNote.<taxonomy>.attach`
- `questionNote.<taxonomy>.detach`
- `questionNote.<taxonomy>.getTerms`
- `questionNote.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `questionNote.subtables.<child>.create`
- `questionNote.subtables.<child>.update`
- `questionNote.subtables.<child>.delete`
- `questionNote.subtables.<child>.get`
- `questionNote.subtables.<child>.list` (submany only)

This model is a subtable: yes (parent model: q)

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `questionNote.relations.<relation>.attach`
- `questionNote.relations.<relation>.detach`
- `questionNote.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `questionNote.typesense.resource`
- `questionNote.typesense.list`
- `questionNote.typesense.refresh`
- `questionNote.typesense.count`
- `questionNote.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/questionNote.ts`

Wrapper router:
- `server/trpc/routers/questionNote.ts`

---

End of document.
