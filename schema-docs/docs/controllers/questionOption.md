Title: QuestionOption Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **QuestionOption controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **QuestionOption controller** is a front-end helper that wraps the TRPC router for the `questionOption` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/questionOption.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/questionOption.ts`

Custom overrides (project side):
- `schema/controllers/questionOption.ts` (or `app/schema/controllers/questionOption.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedQuestionOptionController()`
- `extendQuestionOptionController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { QuestionOptionController } from '@schema/controllers/questionOption'

export const extendQuestionOptionController = (base: QuestionOptionController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'qOption', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- label (required)

Optional fields:
- optionKey
- correct
- order

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `questionOption.create`

### update(id, payload)
- TRPC endpoint: `questionOption.update`

### delete(id)
- TRPC endpoint: `questionOption.delete`

### get(id, resourceKey)
- TRPC endpoint: `questionOption.resource`
- Known resource keys: None

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `questionOption.<taxonomy>.createTaxonomy`
- `questionOption.<taxonomy>.addTerm`
- `questionOption.<taxonomy>.removeTerm`
- `questionOption.<taxonomy>.attach`
- `questionOption.<taxonomy>.detach`
- `questionOption.<taxonomy>.getTerms`
- `questionOption.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
- questionOptionRecord

Endpoints:
- `questionOption.subtables.<child>.create`
- `questionOption.subtables.<child>.update`
- `questionOption.subtables.<child>.delete`
- `questionOption.subtables.<child>.get`
- `questionOption.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `questionOption.relations.<relation>.attach`
- `questionOption.relations.<relation>.detach`
- `questionOption.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `questionOption.typesense.resource`
- `questionOption.typesense.list`
- `questionOption.typesense.refresh`
- `questionOption.typesense.count`
- `questionOption.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/questionOption.ts`

Wrapper router:
- `server/trpc/routers/questionOption.ts`

---

End of document.
