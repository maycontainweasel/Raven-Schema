Title: QuestionOptionRecord Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **QuestionOptionRecord controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **QuestionOptionRecord controller** is a front-end helper that wraps the TRPC router for the `questionOptionRecord` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/questionOptionRecord.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/questionOptionRecord.ts`

Custom overrides (project side):
- `schema/controllers/questionOptionRecord.ts` (or `app/schema/controllers/questionOptionRecord.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedQuestionOptionRecordController()`
- `extendQuestionOptionRecordController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { QuestionOptionRecordController } from '@schema/controllers/questionOptionRecord'

export const extendQuestionOptionRecordController = (base: QuestionOptionRecordController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'qor', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- q (required)
- qOption (required)

Optional fields:
- attempts
- noCorrect
- noIncorrect
- percCorrect

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `questionOptionRecord.create`

### update(id, payload)
- TRPC endpoint: `questionOptionRecord.update`

### delete(id)
- TRPC endpoint: `questionOptionRecord.delete`

### get(id, resourceKey)
- TRPC endpoint: `questionOptionRecord.resource`
- Known resource keys: None

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `questionOptionRecord.<taxonomy>.createTaxonomy`
- `questionOptionRecord.<taxonomy>.addTerm`
- `questionOptionRecord.<taxonomy>.removeTerm`
- `questionOptionRecord.<taxonomy>.attach`
- `questionOptionRecord.<taxonomy>.detach`
- `questionOptionRecord.<taxonomy>.getTerms`
- `questionOptionRecord.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `questionOptionRecord.subtables.<child>.create`
- `questionOptionRecord.subtables.<child>.update`
- `questionOptionRecord.subtables.<child>.delete`
- `questionOptionRecord.subtables.<child>.get`
- `questionOptionRecord.subtables.<child>.list` (submany only)

This model is a subtable: yes (parent model: qOption)

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `questionOptionRecord.relations.<relation>.attach`
- `questionOptionRecord.relations.<relation>.detach`
- `questionOptionRecord.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `questionOptionRecord.typesense.resource`
- `questionOptionRecord.typesense.list`
- `questionOptionRecord.typesense.refresh`
- `questionOptionRecord.typesense.count`
- `questionOptionRecord.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/questionOptionRecord.ts`

Wrapper router:
- `server/trpc/routers/questionOptionRecord.ts`

---

End of document.
