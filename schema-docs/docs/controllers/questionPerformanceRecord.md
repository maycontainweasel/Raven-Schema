Title: QuestionPerformanceRecord Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **QuestionPerformanceRecord controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **QuestionPerformanceRecord controller** is a front-end helper that wraps the TRPC router for the `questionPerformanceRecord` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/questionPerformanceRecord.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/questionPerformanceRecord.ts`

Custom overrides (project side):
- `schema/controllers/questionPerformanceRecord.ts` (or `app/schema/controllers/questionPerformanceRecord.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedQuestionPerformanceRecordController()`
- `extendQuestionPerformanceRecordController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { QuestionPerformanceRecordController } from '@schema/controllers/questionPerformanceRecord'

export const extendQuestionPerformanceRecordController = (base: QuestionPerformanceRecordController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'qpr', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
None

Optional fields:
- attempts
- noCorrect
- noIncorrect
- percCorrect
- totalTime
- averageTime

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `questionPerformanceRecord.create`

### update(id, payload)
- TRPC endpoint: `questionPerformanceRecord.update`

### delete(id)
- TRPC endpoint: `questionPerformanceRecord.delete`

### get(id, resourceKey)
- TRPC endpoint: `questionPerformanceRecord.resource`
- Known resource keys: None

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `questionPerformanceRecord.<taxonomy>.createTaxonomy`
- `questionPerformanceRecord.<taxonomy>.addTerm`
- `questionPerformanceRecord.<taxonomy>.removeTerm`
- `questionPerformanceRecord.<taxonomy>.attach`
- `questionPerformanceRecord.<taxonomy>.detach`
- `questionPerformanceRecord.<taxonomy>.getTerms`
- `questionPerformanceRecord.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `questionPerformanceRecord.subtables.<child>.create`
- `questionPerformanceRecord.subtables.<child>.update`
- `questionPerformanceRecord.subtables.<child>.delete`
- `questionPerformanceRecord.subtables.<child>.get`
- `questionPerformanceRecord.subtables.<child>.list` (submany only)

This model is a subtable: yes (parent model: q)

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `questionPerformanceRecord.relations.<relation>.attach`
- `questionPerformanceRecord.relations.<relation>.detach`
- `questionPerformanceRecord.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `questionPerformanceRecord.typesense.resource`
- `questionPerformanceRecord.typesense.list`
- `questionPerformanceRecord.typesense.refresh`
- `questionPerformanceRecord.typesense.count`
- `questionPerformanceRecord.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/questionPerformanceRecord.ts`

Wrapper router:
- `server/trpc/routers/questionPerformanceRecord.ts`

---

End of document.
