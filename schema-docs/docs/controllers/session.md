Title: Session Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **Session controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **Session controller** is a front-end helper that wraps the TRPC router for the `session` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/session.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/session.ts`

Custom overrides (project side):
- `schema/controllers/session.ts` (or `app/schema/controllers/session.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedSessionController()`
- `extendSessionController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { SessionController } from '@schema/controllers/session'

export const extendSessionController = (base: SessionController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 's', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- uuid (required)
- title (required)
- mode (required)
- state (required)
- active (required)
- dateCreated (required)
- qTotal (required)
- familiarity (required)
- u (required)

Optional fields:
- index
- exams
- categories
- timing
- qids
- qidsInit

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `session.create`

### update(id, payload)
- TRPC endpoint: `session.update`

### delete(id)
- TRPC endpoint: `session.delete`

### get(id, resourceKey)
- TRPC endpoint: `session.resource`
- Known resource keys: Admin

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `session.<taxonomy>.createTaxonomy`
- `session.<taxonomy>.addTerm`
- `session.<taxonomy>.removeTerm`
- `session.<taxonomy>.attach`
- `session.<taxonomy>.detach`
- `session.<taxonomy>.getTerms`
- `session.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `session.subtables.<child>.create`
- `session.subtables.<child>.update`
- `session.subtables.<child>.delete`
- `session.subtables.<child>.get`
- `session.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
- questionAttempt
- question
- questionAttempt
- sessionPerformanceRecord
- sessionLog
- qt
- sessionCategoryRecord
- user
- exam

Endpoints:
- `session.relations.<relation>.attach`
- `session.relations.<relation>.detach`
- `session.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: yes

Endpoints (if enabled):
- `session.typesense.resource`
- `session.typesense.list`
- `session.typesense.refresh`
- `session.typesense.count`
- `session.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/session.ts`

Wrapper router:
- `server/trpc/routers/session.ts`

---

End of document.
