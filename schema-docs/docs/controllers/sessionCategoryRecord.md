Title: SessionCategoryRecord Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **SessionCategoryRecord controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **SessionCategoryRecord controller** is a front-end helper that wraps the TRPC router for the `sessionCategoryRecord` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/sessionCategoryRecord.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/sessionCategoryRecord.ts`

Custom overrides (project side):
- `schema/controllers/sessionCategoryRecord.ts` (or `app/schema/controllers/sessionCategoryRecord.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedSessionCategoryRecordController()`
- `extendSessionCategoryRecordController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { SessionCategoryRecordController } from '@schema/controllers/sessionCategoryRecord'

export const extendSessionCategoryRecordController = (base: SessionCategoryRecordController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'scr', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- s (required)
- qt (required)

Optional fields:
- attempts
- noCorrect
- noIncorrect
- percCorrect
- averageTime
- totalTime

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `sessionCategoryRecord.create`

### update(id, payload)
- TRPC endpoint: `sessionCategoryRecord.update`

### delete(id)
- TRPC endpoint: `sessionCategoryRecord.delete`

### get(id, resourceKey)
- TRPC endpoint: `sessionCategoryRecord.resource`
- Known resource keys: None

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `sessionCategoryRecord.<taxonomy>.createTaxonomy`
- `sessionCategoryRecord.<taxonomy>.addTerm`
- `sessionCategoryRecord.<taxonomy>.removeTerm`
- `sessionCategoryRecord.<taxonomy>.attach`
- `sessionCategoryRecord.<taxonomy>.detach`
- `sessionCategoryRecord.<taxonomy>.getTerms`
- `sessionCategoryRecord.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `sessionCategoryRecord.subtables.<child>.create`
- `sessionCategoryRecord.subtables.<child>.update`
- `sessionCategoryRecord.subtables.<child>.delete`
- `sessionCategoryRecord.subtables.<child>.get`
- `sessionCategoryRecord.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
- session

Endpoints:
- `sessionCategoryRecord.relations.<relation>.attach`
- `sessionCategoryRecord.relations.<relation>.detach`
- `sessionCategoryRecord.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `sessionCategoryRecord.typesense.resource`
- `sessionCategoryRecord.typesense.list`
- `sessionCategoryRecord.typesense.refresh`
- `sessionCategoryRecord.typesense.count`
- `sessionCategoryRecord.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/sessionCategoryRecord.ts`

Wrapper router:
- `server/trpc/routers/sessionCategoryRecord.ts`

---

End of document.
