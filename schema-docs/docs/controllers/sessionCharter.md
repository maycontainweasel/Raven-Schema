Title: SessionCharter Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **SessionCharter controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **SessionCharter controller** is a front-end helper that wraps the TRPC router for the `sessionCharter` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/sessionCharter.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/sessionCharter.ts`

Custom overrides (project side):
- `schema/controllers/sessionCharter.ts` (or `app/schema/controllers/sessionCharter.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedSessionCharterController()`
- `extendSessionCharterController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { SessionCharterController } from '@schema/controllers/sessionCharter'

export const extendSessionCharterController = (base: SessionCharterController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'sc', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- s (required)
- mode (required)
- familiarity (required)
- qTotal (required)

Optional fields:
- exams
- categories
- timing

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `sessionCharter.create`

### update(id, payload)
- TRPC endpoint: `sessionCharter.update`

### delete(id)
- TRPC endpoint: `sessionCharter.delete`

### get(id, resourceKey)
- TRPC endpoint: `sessionCharter.resource`
- Known resource keys: None

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `sessionCharter.<taxonomy>.createTaxonomy`
- `sessionCharter.<taxonomy>.addTerm`
- `sessionCharter.<taxonomy>.removeTerm`
- `sessionCharter.<taxonomy>.attach`
- `sessionCharter.<taxonomy>.detach`
- `sessionCharter.<taxonomy>.getTerms`
- `sessionCharter.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `sessionCharter.subtables.<child>.create`
- `sessionCharter.subtables.<child>.update`
- `sessionCharter.subtables.<child>.delete`
- `sessionCharter.subtables.<child>.get`
- `sessionCharter.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `sessionCharter.relations.<relation>.attach`
- `sessionCharter.relations.<relation>.detach`
- `sessionCharter.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `sessionCharter.typesense.resource`
- `sessionCharter.typesense.list`
- `sessionCharter.typesense.refresh`
- `sessionCharter.typesense.count`
- `sessionCharter.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/sessionCharter.ts`

Wrapper router:
- `server/trpc/routers/sessionCharter.ts`

---

End of document.
