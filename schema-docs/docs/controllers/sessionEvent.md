Title: SessionEvent Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **SessionEvent controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **SessionEvent controller** is a front-end helper that wraps the TRPC router for the `sessionEvent` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/sessionEvent.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/sessionEvent.ts`

Custom overrides (project side):
- `schema/controllers/sessionEvent.ts` (or `app/schema/controllers/sessionEvent.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedSessionEventController()`
- `extendSessionEventController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { SessionEventController } from '@schema/controllers/sessionEvent'

export const extendSessionEventController = (base: SessionEventController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'se', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- action (required)
- time (required)

Optional fields:
None

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `sessionEvent.create`

### update(id, payload)
- TRPC endpoint: `sessionEvent.update`

### delete(id)
- TRPC endpoint: `sessionEvent.delete`

### get(id, resourceKey)
- TRPC endpoint: `sessionEvent.resource`
- Known resource keys: None

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `sessionEvent.<taxonomy>.createTaxonomy`
- `sessionEvent.<taxonomy>.addTerm`
- `sessionEvent.<taxonomy>.removeTerm`
- `sessionEvent.<taxonomy>.attach`
- `sessionEvent.<taxonomy>.detach`
- `sessionEvent.<taxonomy>.getTerms`
- `sessionEvent.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `sessionEvent.subtables.<child>.create`
- `sessionEvent.subtables.<child>.update`
- `sessionEvent.subtables.<child>.delete`
- `sessionEvent.subtables.<child>.get`
- `sessionEvent.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `sessionEvent.relations.<relation>.attach`
- `sessionEvent.relations.<relation>.detach`
- `sessionEvent.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `sessionEvent.typesense.resource`
- `sessionEvent.typesense.list`
- `sessionEvent.typesense.refresh`
- `sessionEvent.typesense.count`
- `sessionEvent.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/sessionEvent.ts`

Wrapper router:
- `server/trpc/routers/sessionEvent.ts`

---

End of document.
