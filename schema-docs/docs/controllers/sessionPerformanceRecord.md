Title: SessionPerformanceRecord Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **SessionPerformanceRecord controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **SessionPerformanceRecord controller** is a front-end helper that wraps the TRPC router for the `sessionPerformanceRecord` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/sessionPerformanceRecord.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/sessionPerformanceRecord.ts`

Custom overrides (project side):
- `schema/controllers/sessionPerformanceRecord.ts` (or `app/schema/controllers/sessionPerformanceRecord.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedSessionPerformanceRecordController()`
- `extendSessionPerformanceRecordController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { SessionPerformanceRecordController } from '@schema/controllers/sessionPerformanceRecord'

export const extendSessionPerformanceRecordController = (base: SessionPerformanceRecordController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'spr', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- s (required)
- u (required)
- qTotal (required)

Optional fields:
- attempts
- noCorrect
- noIncorrect
- noComplete
- noIncomplete
- percComplete
- percCorrect
- percCorrectAttempted
- totalSessionTime
- averageTimePerQ

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `sessionPerformanceRecord.create`

### update(id, payload)
- TRPC endpoint: `sessionPerformanceRecord.update`

### delete(id)
- TRPC endpoint: `sessionPerformanceRecord.delete`

### get(id, resourceKey)
- TRPC endpoint: `sessionPerformanceRecord.resource`
- Known resource keys: None

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `sessionPerformanceRecord.<taxonomy>.createTaxonomy`
- `sessionPerformanceRecord.<taxonomy>.addTerm`
- `sessionPerformanceRecord.<taxonomy>.removeTerm`
- `sessionPerformanceRecord.<taxonomy>.attach`
- `sessionPerformanceRecord.<taxonomy>.detach`
- `sessionPerformanceRecord.<taxonomy>.getTerms`
- `sessionPerformanceRecord.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `sessionPerformanceRecord.subtables.<child>.create`
- `sessionPerformanceRecord.subtables.<child>.update`
- `sessionPerformanceRecord.subtables.<child>.delete`
- `sessionPerformanceRecord.subtables.<child>.get`
- `sessionPerformanceRecord.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
- session

Endpoints:
- `sessionPerformanceRecord.relations.<relation>.attach`
- `sessionPerformanceRecord.relations.<relation>.detach`
- `sessionPerformanceRecord.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `sessionPerformanceRecord.typesense.resource`
- `sessionPerformanceRecord.typesense.list`
- `sessionPerformanceRecord.typesense.refresh`
- `sessionPerformanceRecord.typesense.count`
- `sessionPerformanceRecord.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/sessionPerformanceRecord.ts`

Wrapper router:
- `server/trpc/routers/sessionPerformanceRecord.ts`

---

End of document.
