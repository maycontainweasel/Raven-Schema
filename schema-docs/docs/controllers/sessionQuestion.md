Title: SessionQuestion Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **SessionQuestion controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **SessionQuestion controller** is a front-end helper that wraps the TRPC router for the `sessionQuestion` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/sessionQuestion.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/sessionQuestion.ts`

Custom overrides (project side):
- `schema/controllers/sessionQuestion.ts` (or `app/schema/controllers/sessionQuestion.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedSessionQuestionController()`
- `extendSessionQuestionController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { SessionQuestionController } from '@schema/controllers/sessionQuestion'

export const extendSessionQuestionController = (base: SessionQuestionController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'SessionQuestions', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- s (required)
- q (required)
- order (required)

Optional fields:
- flagged
- complete
- totalTime

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `sessionQuestion.create`

### update(id, payload)
- TRPC endpoint: `sessionQuestion.update`

### delete(id)
- TRPC endpoint: `sessionQuestion.delete`

### get(id, resourceKey)
- TRPC endpoint: `sessionQuestion.resource`
- Known resource keys: None

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `sessionQuestion.<taxonomy>.createTaxonomy`
- `sessionQuestion.<taxonomy>.addTerm`
- `sessionQuestion.<taxonomy>.removeTerm`
- `sessionQuestion.<taxonomy>.attach`
- `sessionQuestion.<taxonomy>.detach`
- `sessionQuestion.<taxonomy>.getTerms`
- `sessionQuestion.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `sessionQuestion.subtables.<child>.create`
- `sessionQuestion.subtables.<child>.update`
- `sessionQuestion.subtables.<child>.delete`
- `sessionQuestion.subtables.<child>.get`
- `sessionQuestion.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `sessionQuestion.relations.<relation>.attach`
- `sessionQuestion.relations.<relation>.detach`
- `sessionQuestion.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `sessionQuestion.typesense.resource`
- `sessionQuestion.typesense.list`
- `sessionQuestion.typesense.refresh`
- `sessionQuestion.typesense.count`
- `sessionQuestion.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/sessionQuestion.ts`

Wrapper router:
- `server/trpc/routers/sessionQuestion.ts`

---

End of document.
