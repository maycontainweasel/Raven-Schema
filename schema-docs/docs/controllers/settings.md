Title: InstanceSettings Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **InstanceSettings controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **InstanceSettings controller** is a front-end helper that wraps the TRPC router for the `settings` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/settings.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/settings.ts`

Custom overrides (project side):
- `schema/controllers/settings.ts` (or `app/schema/controllers/settings.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedInstanceSettingsController()`
- `extendInstanceSettingsController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { InstanceSettingsController } from '@schema/controllers/settings'

export const extendInstanceSettingsController = (base: InstanceSettingsController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'instanceSettings', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
None

Optional fields:
- currencies
- defaultCurrency

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `settings.create`

### update(id, payload)
- TRPC endpoint: `settings.update`

### delete(id)
- TRPC endpoint: `settings.delete`

### get(id, resourceKey)
- TRPC endpoint: `settings.resource`
- Known resource keys: None

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `settings.<taxonomy>.createTaxonomy`
- `settings.<taxonomy>.addTerm`
- `settings.<taxonomy>.removeTerm`
- `settings.<taxonomy>.attach`
- `settings.<taxonomy>.detach`
- `settings.<taxonomy>.getTerms`
- `settings.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `settings.subtables.<child>.create`
- `settings.subtables.<child>.update`
- `settings.subtables.<child>.delete`
- `settings.subtables.<child>.get`
- `settings.subtables.<child>.list` (submany only)

This model is a subtable: yes (parent model: instance)

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `settings.relations.<relation>.attach`
- `settings.relations.<relation>.detach`
- `settings.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `settings.typesense.resource`
- `settings.typesense.list`
- `settings.typesense.refresh`
- `settings.typesense.count`
- `settings.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/settings.ts`

Wrapper router:
- `server/trpc/routers/settings.ts`

---

End of document.
