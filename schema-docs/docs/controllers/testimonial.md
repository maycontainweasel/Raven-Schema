Title: Testimonial Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **Testimonial controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **Testimonial controller** is a front-end helper that wraps the TRPC router for the `testimonial` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/testimonial.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/testimonial.ts`

Custom overrides (project side):
- `schema/controllers/testimonial.ts` (or `app/schema/controllers/testimonial.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedTestimonialController()`
- `extendTestimonialController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { TestimonialController } from '@schema/controllers/testimonial'

export const extendTestimonialController = (base: TestimonialController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'testimonial', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- name (required)
- quote (required)
- active (required)

Optional fields:
- role
- organisation
- avatar
- rating
- order
- instances

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `testimonial.create`

### update(id, payload)
- TRPC endpoint: `testimonial.update`

### delete(id)
- TRPC endpoint: `testimonial.delete`

### get(id, resourceKey)
- TRPC endpoint: `testimonial.resource`
- Known resource keys: Admin, Public

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `testimonial.<taxonomy>.createTaxonomy`
- `testimonial.<taxonomy>.addTerm`
- `testimonial.<taxonomy>.removeTerm`
- `testimonial.<taxonomy>.attach`
- `testimonial.<taxonomy>.detach`
- `testimonial.<taxonomy>.getTerms`
- `testimonial.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `testimonial.subtables.<child>.create`
- `testimonial.subtables.<child>.update`
- `testimonial.subtables.<child>.delete`
- `testimonial.subtables.<child>.get`
- `testimonial.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `testimonial.relations.<relation>.attach`
- `testimonial.relations.<relation>.detach`
- `testimonial.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: yes

Endpoints (if enabled):
- `testimonial.typesense.resource`
- `testimonial.typesense.list`
- `testimonial.typesense.refresh`
- `testimonial.typesense.count`
- `testimonial.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/testimonial.ts`

Wrapper router:
- `server/trpc/routers/testimonial.ts`

---

End of document.
