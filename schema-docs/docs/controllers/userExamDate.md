Title: UserExamDate Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **UserExamDate controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **UserExamDate controller** is a front-end helper that wraps the TRPC router for the `userExamDate` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/userExamDate.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/userExamDate.ts`

Custom overrides (project side):
- `schema/controllers/userExamDate.ts` (or `app/schema/controllers/userExamDate.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedUserExamDateController()`
- `extendUserExamDateController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { UserExamDateController } from '@schema/controllers/userExamDate'

export const extendUserExamDateController = (base: UserExamDateController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'uExamDate', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- u (required)
- exam (required)
- examDate (required)

Optional fields:
- createdAt
- updatedAt
- order

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `userExamDate.create`

### update(id, payload)
- TRPC endpoint: `userExamDate.update`

### delete(id)
- TRPC endpoint: `userExamDate.delete`

### get(id, resourceKey)
- TRPC endpoint: `userExamDate.resource`
- Known resource keys: Single

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `userExamDate.<taxonomy>.createTaxonomy`
- `userExamDate.<taxonomy>.addTerm`
- `userExamDate.<taxonomy>.removeTerm`
- `userExamDate.<taxonomy>.attach`
- `userExamDate.<taxonomy>.detach`
- `userExamDate.<taxonomy>.getTerms`
- `userExamDate.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `userExamDate.subtables.<child>.create`
- `userExamDate.subtables.<child>.update`
- `userExamDate.subtables.<child>.delete`
- `userExamDate.subtables.<child>.get`
- `userExamDate.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `userExamDate.relations.<relation>.attach`
- `userExamDate.relations.<relation>.detach`
- `userExamDate.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `userExamDate.typesense.resource`
- `userExamDate.typesense.list`
- `userExamDate.typesense.refresh`
- `userExamDate.typesense.count`
- `userExamDate.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/userExamDate.ts`

Wrapper router:
- `server/trpc/routers/userExamDate.ts`

---

End of document.
