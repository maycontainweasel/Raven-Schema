Title: UserProfile Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **UserProfile controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **UserProfile controller** is a front-end helper that wraps the TRPC router for the `userProfile` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/userProfile.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/userProfile.ts`

Custom overrides (project side):
- `schema/controllers/userProfile.ts` (or `app/schema/controllers/userProfile.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedUserProfileController()`
- `extendUserProfileController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { UserProfileController } from '@schema/controllers/userProfile'

export const extendUserProfileController = (base: UserProfileController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'uProfile', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
None

Optional fields:
- address
- avatar
- bio
- city
- country
- cover
- credentials
- levelOfTraining
- medicalSchool
- position
- postalCode
- primaryAffiliation
- website
- yearOfGraduation

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `userProfile.create`

### update(id, payload)
- TRPC endpoint: `userProfile.update`

### delete(id)
- TRPC endpoint: `userProfile.delete`

### get(id, resourceKey)
- TRPC endpoint: `userProfile.resource`
- Known resource keys: None

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `userProfile.<taxonomy>.createTaxonomy`
- `userProfile.<taxonomy>.addTerm`
- `userProfile.<taxonomy>.removeTerm`
- `userProfile.<taxonomy>.attach`
- `userProfile.<taxonomy>.detach`
- `userProfile.<taxonomy>.getTerms`
- `userProfile.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `userProfile.subtables.<child>.create`
- `userProfile.subtables.<child>.update`
- `userProfile.subtables.<child>.delete`
- `userProfile.subtables.<child>.get`
- `userProfile.subtables.<child>.list` (submany only)

This model is a subtable: yes (parent model: u)

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `userProfile.relations.<relation>.attach`
- `userProfile.relations.<relation>.detach`
- `userProfile.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `userProfile.typesense.resource`
- `userProfile.typesense.list`
- `userProfile.typesense.refresh`
- `userProfile.typesense.count`
- `userProfile.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/userProfile.ts`

Wrapper router:
- `server/trpc/routers/userProfile.ts`

---

End of document.
