Title: UserQuestionNote Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **UserQuestionNote controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **UserQuestionNote controller** is a front-end helper that wraps the TRPC router for the `userQuestionNote` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/userQuestionNote.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/userQuestionNote.ts`

Custom overrides (project side):
- `schema/controllers/userQuestionNote.ts` (or `app/schema/controllers/userQuestionNote.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedUserQuestionNoteController()`
- `extendUserQuestionNoteController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { UserQuestionNoteController } from '@schema/controllers/userQuestionNote'

export const extendUserQuestionNoteController = (base: UserQuestionNoteController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'uqnote', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- u (required)
- q (required)

Optional fields:
- note
- order

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `userQuestionNote.create`

### update(id, payload)
- TRPC endpoint: `userQuestionNote.update`

### delete(id)
- TRPC endpoint: `userQuestionNote.delete`

### get(id, resourceKey)
- TRPC endpoint: `userQuestionNote.resource`
- Known resource keys: None

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `userQuestionNote.<taxonomy>.createTaxonomy`
- `userQuestionNote.<taxonomy>.addTerm`
- `userQuestionNote.<taxonomy>.removeTerm`
- `userQuestionNote.<taxonomy>.attach`
- `userQuestionNote.<taxonomy>.detach`
- `userQuestionNote.<taxonomy>.getTerms`
- `userQuestionNote.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `userQuestionNote.subtables.<child>.create`
- `userQuestionNote.subtables.<child>.update`
- `userQuestionNote.subtables.<child>.delete`
- `userQuestionNote.subtables.<child>.get`
- `userQuestionNote.subtables.<child>.list` (submany only)

This model is a subtable: yes (parent model: q)

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `userQuestionNote.relations.<relation>.attach`
- `userQuestionNote.relations.<relation>.detach`
- `userQuestionNote.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `userQuestionNote.typesense.resource`
- `userQuestionNote.typesense.list`
- `userQuestionNote.typesense.refresh`
- `userQuestionNote.typesense.count`
- `userQuestionNote.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/userQuestionNote.ts`

Wrapper router:
- `server/trpc/routers/userQuestionNote.ts`

---

End of document.
