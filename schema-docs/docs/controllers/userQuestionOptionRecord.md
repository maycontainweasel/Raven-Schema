Title: UserQuestionOptionRecord Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **UserQuestionOptionRecord controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **UserQuestionOptionRecord controller** is a front-end helper that wraps the TRPC router for the `userQuestionOptionRecord` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/userQuestionOptionRecord.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/userQuestionOptionRecord.ts`

Custom overrides (project side):
- `schema/controllers/userQuestionOptionRecord.ts` (or `app/schema/controllers/userQuestionOptionRecord.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedUserQuestionOptionRecordController()`
- `extendUserQuestionOptionRecordController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { UserQuestionOptionRecordController } from '@schema/controllers/userQuestionOptionRecord'

export const extendUserQuestionOptionRecordController = (base: UserQuestionOptionRecordController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'uqor', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- u (required)
- qOption (required)

Optional fields:
- attempts
- correct
- incorrect
- percCorrect
- order

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `userQuestionOptionRecord.create`

### update(id, payload)
- TRPC endpoint: `userQuestionOptionRecord.update`

### delete(id)
- TRPC endpoint: `userQuestionOptionRecord.delete`

### get(id, resourceKey)
- TRPC endpoint: `userQuestionOptionRecord.resource`
- Known resource keys: None

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `userQuestionOptionRecord.<taxonomy>.createTaxonomy`
- `userQuestionOptionRecord.<taxonomy>.addTerm`
- `userQuestionOptionRecord.<taxonomy>.removeTerm`
- `userQuestionOptionRecord.<taxonomy>.attach`
- `userQuestionOptionRecord.<taxonomy>.detach`
- `userQuestionOptionRecord.<taxonomy>.getTerms`
- `userQuestionOptionRecord.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `userQuestionOptionRecord.subtables.<child>.create`
- `userQuestionOptionRecord.subtables.<child>.update`
- `userQuestionOptionRecord.subtables.<child>.delete`
- `userQuestionOptionRecord.subtables.<child>.get`
- `userQuestionOptionRecord.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `userQuestionOptionRecord.relations.<relation>.attach`
- `userQuestionOptionRecord.relations.<relation>.detach`
- `userQuestionOptionRecord.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `userQuestionOptionRecord.typesense.resource`
- `userQuestionOptionRecord.typesense.list`
- `userQuestionOptionRecord.typesense.refresh`
- `userQuestionOptionRecord.typesense.count`
- `userQuestionOptionRecord.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/userQuestionOptionRecord.ts`

Wrapper router:
- `server/trpc/routers/userQuestionOptionRecord.ts`

---

End of document.
