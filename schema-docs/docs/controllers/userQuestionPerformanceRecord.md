Title: UserQuestionPerformanceRecord Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **UserQuestionPerformanceRecord controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **UserQuestionPerformanceRecord controller** is a front-end helper that wraps the TRPC router for the `userQuestionPerformanceRecord` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/userQuestionPerformanceRecord.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/userQuestionPerformanceRecord.ts`

Custom overrides (project side):
- `schema/controllers/userQuestionPerformanceRecord.ts` (or `app/schema/controllers/userQuestionPerformanceRecord.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedUserQuestionPerformanceRecordController()`
- `extendUserQuestionPerformanceRecordController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { UserQuestionPerformanceRecordController } from '@schema/controllers/userQuestionPerformanceRecord'

export const extendUserQuestionPerformanceRecordController = (base: UserQuestionPerformanceRecordController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'uqpr', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
None

Optional fields:
- attempts
- noCorrect
- noIncorrect
- percCorrect
- totalTime
- averageTime
- order

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `userQuestionPerformanceRecord.create`

### update(id, payload)
- TRPC endpoint: `userQuestionPerformanceRecord.update`

### delete(id)
- TRPC endpoint: `userQuestionPerformanceRecord.delete`

### get(id, resourceKey)
- TRPC endpoint: `userQuestionPerformanceRecord.resource`
- Known resource keys: None

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `userQuestionPerformanceRecord.<taxonomy>.createTaxonomy`
- `userQuestionPerformanceRecord.<taxonomy>.addTerm`
- `userQuestionPerformanceRecord.<taxonomy>.removeTerm`
- `userQuestionPerformanceRecord.<taxonomy>.attach`
- `userQuestionPerformanceRecord.<taxonomy>.detach`
- `userQuestionPerformanceRecord.<taxonomy>.getTerms`
- `userQuestionPerformanceRecord.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `userQuestionPerformanceRecord.subtables.<child>.create`
- `userQuestionPerformanceRecord.subtables.<child>.update`
- `userQuestionPerformanceRecord.subtables.<child>.delete`
- `userQuestionPerformanceRecord.subtables.<child>.get`
- `userQuestionPerformanceRecord.subtables.<child>.list` (submany only)

This model is a subtable: yes (parent model: q)

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `userQuestionPerformanceRecord.relations.<relation>.attach`
- `userQuestionPerformanceRecord.relations.<relation>.detach`
- `userQuestionPerformanceRecord.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `userQuestionPerformanceRecord.typesense.resource`
- `userQuestionPerformanceRecord.typesense.list`
- `userQuestionPerformanceRecord.typesense.refresh`
- `userQuestionPerformanceRecord.typesense.count`
- `userQuestionPerformanceRecord.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/userQuestionPerformanceRecord.ts`

Wrapper router:
- `server/trpc/routers/userQuestionPerformanceRecord.ts`

---

End of document.
